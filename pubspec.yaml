name: gyawun_metadata_plugin
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
#dependencies:
  # path: ^1.9.0

publish_to: none

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dart_eval: ^0.8.4
  eval_annotation: ^0.8.1
  http: ^1.6.0
  mocktail: ^1.0.0
dependencies:
  gyawun_metadata_sdk:
    path: ../gyawun_metadata_sdk
  path: ^1.9.1
